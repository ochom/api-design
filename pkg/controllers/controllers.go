package controllers

// Controller is the interface that wraps the basic controller methods.
type Controller struct{}

// NewController creates a new controller instance.
func NewController() *Controller {
	return &Controller{}
}
